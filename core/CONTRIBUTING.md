# Contributing to SQL Academy

Thank you for your interest in contributing to SQL Academy!

## Development Setup

1. Clone the repository
2. Run `npm install`
3. Run `npm run dev` to launch the local Vite development server
4. Run `npm test` to validate all 202 bundled SQL queries across the curriculum
5. Run `npm run build` to verify type checking and bundling before submitting a pull request
